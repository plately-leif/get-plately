# Ensure consistent package installations
auto-install-peers=true
legacy-peer-deps=true
strict-peer-dependencies=false
prefer-offline=true
# Cache settings
cache=.npm-cache
# Disable package-lock to prevent conflicts
tmp=.npm-tmp
# Disable package-lock to prevent conflicts
package-lock=false
# Set Node.js version
engine-strict=true
